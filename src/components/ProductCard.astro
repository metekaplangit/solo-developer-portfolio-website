---
import Badge from './Badge.astro';
import type { Product } from '../content/schema';
import {
  visibleStoreLinks,
  hasStoreLinks,
  storeLabel,
  statusLabel,
} from '../lib/products';
import { canonicalPrivacyUrl } from '../lib/privacy';
import { withBase } from '../lib/url';

interface Props {
  product: Product;
}
const { product } = Astro.props;
const monogram = product.name
  .split(/\s+/)
  .map((w) => w[0])
  .join('')
  .slice(0, 2)
  .toUpperCase();
const stores = visibleStoreLinks(product);
---

<article class="card">
  <div class="card-head">
    <span class="monogram" aria-hidden="true">{monogram}</span>
    <div class="card-titles">
      <h3 class="card-name">
        <a href={withBase(`/apps/${product.slug}/`)}>{product.name}</a>
      </h3>
      <div class="card-tags">
        <Badge label={product.type === 'game' ? 'Game' : 'App'} tone="accent" />
        <Badge label={statusLabel(product.status)} />
      </div>
    </div>
  </div>

  <p class="card-summary">{product.summary}</p>

  <ul class="platforms" aria-label="Platforms">
    {product.platforms.map((p) => <li><Badge label={p} tone="muted" /></li>)}
  </ul>

  <div class="card-links">
    {
      hasStoreLinks(product) ? (
        <ul class="store-links">
          {stores.map((link) => (
            <li>
              <a href={link.url} rel="noopener" target="_blank">
                {storeLabel(link.store)}
                {link.status === 'coming-soon' ? ' (soon)' : ''}
              </a>
            </li>
          ))}
        </ul>
      ) : (
        <p class="unreleased">Not yet available</p>
      )
    }
    <a class="privacy-link" href={withBase(canonicalPrivacyUrl(product))}>
      Privacy<span class="visually-hidden"> for {product.name}</span>
    </a>
  </div>
</article>

<style>
  .card {
    border: 1px solid var(--color-border);
    border-radius: var(--radius);
    background: var(--color-surface);
    padding: var(--space-6);
    display: flex;
    flex-direction: column;
    gap: var(--space-3);
    height: 100%;
  }
  .card-head {
    display: flex;
    align-items: center;
    gap: var(--space-4);
  }
  .monogram {
    display: grid;
    place-items: center;
    width: 3rem;
    height: 3rem;
    border-radius: var(--radius);
    background: var(--color-accent);
    color: var(--color-accent-contrast);
    font-weight: 700;
    flex: none;
  }
  .card-name {
    margin: 0;
    font-size: 1.125rem;
  }
  .card-name a {
    color: var(--color-text);
    text-decoration: none;
  }
  .card-name a:hover {
    text-decoration: underline;
    text-decoration-color: var(--color-accent);
  }
  .card-tags {
    display: flex;
    gap: var(--space-2);
    margin-top: var(--space-1);
  }
  .card-summary {
    margin: 0;
    color: var(--color-muted);
  }
  .platforms,
  .store-links {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-2);
    list-style: none;
    margin: 0;
    padding: 0;
  }
  .card-links {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    justify-content: space-between;
    gap: var(--space-3);
    margin-top: auto;
    padding-top: var(--space-3);
  }
  .unreleased {
    margin: 0;
    color: var(--color-muted);
    font-size: 0.875rem;
  }
  .privacy-link {
    font-size: 0.875rem;
  }
</style>
