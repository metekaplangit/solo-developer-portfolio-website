---
// Baseline scaffold page only. The real home page is built in STEP-0002
// (premium minimal showcase). This exists so the static build is valid at the
// clean baseline and is intentionally free of product content.
const title = 'Solo Developer Portfolio — baseline scaffold';
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="robots" content="noindex" />
    <title>{title}</title>
  </head>
  <body>
    <main>
      <h1>Baseline scaffold</h1>
      <p>
        This is the initialized Astro static baseline. Feature work begins at
        STEP-0001. See <code>docs/STATUS.md</code> for current state.
      </p>
    </main>
  </body>
</html>
