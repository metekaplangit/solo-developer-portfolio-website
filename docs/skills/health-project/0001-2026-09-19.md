Produced by: /health-project, the owner typed the command.
Date: 2026-09-19.
Covers: instructions, documents, links and paths, layout, configuration and simplicity of solo-dev-portfolio-website at 03e0137 (control 22, main, clean).

# Health check — 2026-09-19

Tier: Headless Suite Testing, complete — nothing rendered changed. Before and after: `npm run headless` exit 1 at `npm audit` (issue 0001, red before this run); build, `astro check` (0 errors), 114 unit and 22 built-output tests run by hand, exit 0 both times; control's own 223 tests OK after.

## What was fixed

| # | Lane | What had drifted | What I did | How I know it is safe |
| --- | --- | --- | --- | --- |
| 1 | Configuration | `control/project.py`:95 said deploy.yml publishes from the tag | Comment now says push to main, docs-only skipped | Comment only; `import project` loads; deploy.yml read |
| 2 | Instructions | `CLAUDE.md` live tier pointed at a no-analytics rule "below"; it is above | Now names Non-negotiables above | Read in place |
| 3 | Documents | `PRODUCT.md`:25,43 named only Sole Focus; Magic Notes live since 2026-08-16, featured since 2026-08-17 | Both products named; CTA is the featured product | Content files read (`featured: true` on Magic Notes) |
| 4 | Documents | `PRODUCT.md`:101 called the Lighthouse gate merge-critical; only deploy.yml runs it | Now "before it publishes … blocks the deploy" | `.github/workflows` holds only deploy.yml |
| 5 | Instructions | `docs/CHANGELOG.md`:5-6 told sessions to update it during retired packets and sync with STATUS/ROADMAP | Header says the control's finish writes it | `loop.newest_recorded` still reads v0.52.0 |

Commits: 5df68c1, 751fbae, 4 and 5 share one commit on PRODUCT.md (two findings, one file), a4bc28b.

## Healthy lanes

- Links and paths: every file CLAUDE.md names exists; npm scripts and `scripts/*.mjs` it names exist; screen tags match `tests/screens/site.spec.ts`.
- Layout: healthy.

## Left alone

- `src/lib/site.ts` `headline` ("Brilliant Products for All") is used nowhere (grep of src). Product code — changes what ships.
- `src/pages/support/index.astro` says "nothing is tracked", the promise shape removed on 2026-09-10. Product wording.
- `docs/CHANGELOG.md`:47 empty `## [Unreleased]` heading. Removal; only the retired validator names it.
- `docs/CHANGELOG.md`:11-13 still speaks of Step IDs. Retired record, left.
- `docs/issues/LEDGER.md` says new findings go to GitHub Issues; retired, and `docs/issues/open/` now exists.
- The screen suite covers 9 routes; `/privacy/magic-notes/` has no geometry test. Testing subject, not drift.
- `scripts/probe/height-probe.mjs` is a kept one-off probe (a card's notes say so).
- Folders beside the repo, `../solo-developer-portfolio-website/` and `../ui-health-check-2026-07-18/`, are outside the repository.

## Not mine

- Issue 0001: `npm audit` red, devalue 5.8.1 under astro 7.3.2 (GHSA-9rgm-9g3h-6x36). Stops every card's check.
- Issue 0002: `npm run test:live` red on 9 of 9 routes, the Cloudflare beacon. Documented in CLAUDE.md; dashboard setting.
- 2 build warnings present before and after: Shiki inline styles vs CSP, and `style-src` resources not applying to `style-src-attr`.

* Took 3m 59s.
