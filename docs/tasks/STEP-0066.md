# Task Card — STEP-0066

- **ID:** STEP-0066
- **Type:** docs (record accuracy)
- **One outcome:** `docs/STATUS.md` describes the project as it actually is.
- **Feedback:** AUDIT-0010 (whole-project evaluation, 2026-08-01). Documentation scored 82; every point deducted was drift, not absence.
- **Why now:** STATUS is the first document every session reads (`CLAUDE.md`). Three of its claims were false, and each would have sent the next session at work that was already done, already impossible, or never needed.
- **Prior art:** none sought. This is a factual reconciliation against evidence already gathered; nothing a search could return would change it.
- **Avoid:** deleting the follow-up list. Items #2 (`PolicyArticle` hardcoded prose) and #4 (`noUncheckedIndexedAccess` off, 7 measured errors) are real and stay — #4 in particular describes a latent runtime crash behind a green typecheck.
- **Avoid:** dropping the `STEP-XXXX` token from the "Next action" bullet. The validator resolves the active step from the first one in the file, and STATUS itself records that without one the scan falls through to the trigger-armed STEP-0033 template and fails locally. CI is retired, so nothing would catch it remotely.
- **Approach:** correct the three claims in place with the evidence that retired each, keep the two real ones, refresh the dependency note that STEP-0064 invalidated, and record AUDIT-0009 and AUDIT-0010 in the ledger.
- **Design decision — strike through rather than delete.** A follow-up that turns out to be already-done or moot is itself a finding: it says the record drifted, and deleting the line hides that. Both retired items keep one line saying what closed them.
- **Acceptance criteria:** (1) No claim in STATUS contradicts the live site or the code. (2) `Next action` names a real next step and still carries a `STEP-XXXX` token. (3) AUDIT-0009 and AUDIT-0010 both in `CHECKPOINTS.md`. (4) Validator exits 0.
- **Non-goals:** rewriting ROADMAP's completed-step history; adding changelog entries (internal work does not consume a product version, per `CHANGELOG.md`'s own preamble).
- **Files:** `docs/STATUS.md`, `docs/CHECKPOINTS.md`, `docs/ROADMAP.md`.
- **Evidence method:** `python3 scripts/validate-governance.py` exit code, plus a re-`curl` of the live page for the analytics claim.
- **Proof classification:** machine-verifiable (validator) plus one re-checked external fact.
- **Required checks:** `python3 scripts/validate-governance.py`. No build change.
- **Rollback/containment:** documents only; `git revert -m 1`.
- **Issue links:** AUDIT-0010, AUDIT-0009. No GitHub issue.
- **Evidence after completion:** Validator 44 checks passed, exit 0.

  | Claim | Was | Is |
  |---|---|---|
  | Next action | "one decision is yours" — Cloudflare beacon live, blocked on the owner | Not blocked. `curl` returns one `<script>` (JSON-LD), zero analytics matches; STEP-0065 now enforces the boundary |
  | Follow-up #1 | Home spotlight / catalog lead-row need a `ProductLead` extraction | Closed by STEP-0047; `ProductBand.astro`'s own header records it |
  | Follow-up #3 | About/Support lack the in-content link underline | Moot — every in-content link on both already sits in `.elsewhere` / `.products` / `.privacy-ref`, all covered by the global rule |
  | Follow-ups #2, #4 | Open | Unchanged, still open, #2 now pointed at STEP-0067 |
  | Dependency note | "lockfile remains on Astro 7.0.5… patch updates not mixed in" | Astro 7.1.6, with STEP-0064's reason and proof |
  | Blockers | SMOOTH-0009-2 needs an owner decision | none |

  **The validator caught a step I had not recorded.** The first run failed on `step.status_matches_roadmap :: STEP-0064 missing from ROADMAP.md` — the three internal packets from this run had cards and commits but no roadmap entries. Added for all three; the project's own tooling naming the gap is what found it, not a re-read.

  **Also repaired:** AUDIT-0009 had a report on disk but had never reached the checkpoint ledger. Recorded retroactively, marked as such.
