# Task Card — STEP-0032

- **ID:** STEP-0032
- **Type:** feature (accessibility — gallery text alternative; review-0002 adoption)
- **One outcome:** A visitor who can't or won't operate the carousel can read the whole five-screenshot story as text: a native "Screenshot transcript" disclosure below the gallery listing each shot's title and description in order.
- **Why now:** Review-0002 AI-07 — the one speculative-lane idea worth keeping: zero-JS (native `<details>`), reuses the high-quality alt text already in the content file, and extends the gallery's accessibility story.
- **Acceptance criteria:** (1) Native `<details>/<summary>` — works with JavaScript disabled; collapsed by default; visible focus. (2) Ordered list sourced from the existing screenshot records: a humanized title derived from each file name (e.g. `01-one-quiet-app` → "One quiet app") + the record's alt text — no duplicated prose to maintain. (3) Gallery behavior untouched. (4) `screenshotTitle()` helper unit-tested. (5) Gates green; a11y ≥ 0.95; live-verified.
- **Non-goals:** Visible captions on the live carousel (USER-06, taste-tier/design round); redesigning the gallery.
- **Evidence method:** built-HTML grep (details + all five titles) + preview check + gates + live curl.
- **Proof classification:** machine-verifiable + manual-runtime.
- **Required checks:** `npm run build`; `npm run check`; `npm test`; validator; CI a11y gate; live verification.
- **Issue links:** Adopts review-0002 AI-07. **MC-0016** (Markdown Consistency due at feature step 32) runs alongside.
- **Rollback/containment:** one block in `ScreenshotShowcase.astro` + one helper; trivial revert.
- **Evidence after completion:** Released as v0.32.0. Transcript disclosure live below the gallery with all five humanized titles + alt-text descriptions; `screenshotTitle` unit-tested (42 tests); zero JS added; gates green; deploy through a11y gate; live-verified. MC-0016 run.
