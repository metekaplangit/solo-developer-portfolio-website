# Task Card — STEP-0025

- **ID:** STEP-0025
- **Type:** feature (layout — system-wide alignment) + integrity pass
- **One outcome:** Fix the site-wide "pushed to the left" imbalance by centering every reading column on the shared axis (Direction A, user-chosen from mockups), so content is balanced and aligned across all pages — plus a full-system integrity audit (AUDIT-0005).
- **Why now:** User reported the site looked left-clumped and unbalanced, asked for mockups to choose a direction, and picked **A · Centered column**.
- **Acceptance criteria:** (1) Every `--maxw-prose` reading column and the home hero gets `margin-inline: auto` so it centers within the 68rem shell (home hero, PageHeader, about, support, privacy index + detail/PolicyArticle, detail `.back`/`.detail`/`.detail-body`/`.related`, ScreenshotShowcase). (2) Grid pages stay aligned with their centered header: card grids use `auto-fit` + capped column width + `justify-content: center` (and the catalog section label centers) so one-or-many cards sit centered under the centered header — no centered-header-over-left-grid mismatch. (3) Body text stays left-aligned *inside* its centered column for readability. (4) The centering convention is documented at the `--maxw-prose` token. (5) No horizontal overflow at desktop (1440) or mobile (375); verified centered on home, support/about, catalog, detail, and policy pages. (6) `npm run build`, `npm run check`, `npm test`, validator pass. (7) **AUDIT-0005** (due at feature step 25) run as the integrity pass.
- **Non-goals:** Directions B (tighter shell) or C (centered-aligned hero text) — not chosen; narrowing `--maxw`; changing copy, colors, or components' internals; centering long body text.
- **Evidence method:** command/log (build/check/test/validator) + runtime preview at 1440 and 375 with measured gutters (left ≈ right) on representative pages + no-overflow assertion; before/after screenshots (support page was the clearest offender).
- **Proof classification:** manual-runtime (measured centering + visual) + machine-verifiable (build/tests).
- **Required checks:** `npm run build`; `npm run check`; `npm test`; validator; runtime preview (desktop + mobile).
- **Issue links:** resolves the user-reported left-alignment/balance issue; mockups A/B/C presented, A chosen.
- **Rollback/containment:** work on `feature/STEP-0025-centered-layout`; revert the merge or branch from `v0.24.x`; changes are CSS-only across page/component style blocks + a token comment.
- **Evidence after completion:** Released as v0.25.0. Centered columns confirmed by measured gutters — home hero L347/R358, detail L395/R406, showcase L203/R214 at 1440; support/catalog/detail/policy all centered; **no horizontal overflow** at 1440 or 375. 10 files carry `margin-inline: auto`; catalog + home grids use `auto-fit` + capped width + centered tracks. `npm test` → 36 passed; `npm run check` → 0/0/0; `npm run build` → 8 routes; validator → 40/40. **AUDIT-0005** run (feature step #25).
