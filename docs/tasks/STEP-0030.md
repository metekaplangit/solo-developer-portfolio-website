# Task Card — STEP-0030

- **ID:** STEP-0030
- **Type:** feature (content/UI — purchase-decision information; external design-review remediation)
- **One outcome:** The information a buyer needs at the moment of decision sits next to the download action: a human-readable platform + verified system requirement, and a truthful maker/support trust line — adopting review-0001 findings OPP-04 and OPP-07.
- **Why now:** Review-0001 found the product header renders the raw schema token `macos` (looks internal; answers no compatibility question) and that the site's real trust facts (named maker, 2–3-business-day support reply) live only on separate pages, invisible on the purchase path. Verified: both `[slug].astro` and `ProductCard.astro` badge the raw token; Support page states "I usually reply within 2–3 business days"; About states "the one-person studio of Mete Kaplan".
- **Acceptance criteria:** (1) Platform badges render human labels ("macOS", "iOS", …) everywhere, from one shared label map also used by the JSON-LD builder (no duplicated mapping). (2) The product content carries a verified `requirements` string — "macOS 10.15 or later", confirmed from the live App Store listing — rendered as a small muted line by the header download button; field optional so future products degrade gracefully. (3) A compact trust line appears under the header CTA: "Made by Mete Kaplan · direct support, usually replies in 2–3 business days", linking the existing About and Support pages; subordinate to the download button; claims sourced from those pages verbatim-truthful. (4) Unit test for the platform label map; content test pins the requirements string. (5) All gates green; a11y ≥ 0.95; live-verified.
- **Non-goals:** Inferring Apple-silicon/Intel details (not stated on the listing surface we verified); testimonials or any fabricated social proof; email address exposure outside the Support page; rating JSON-LD.
- **Evidence method:** built-HTML greps (label, requirement, trust line) + preview screenshot + gates + live curl after deploy.
- **Proof classification:** machine-verifiable + manual-runtime.
- **Required checks:** `npm run build`; `npm run check`; `npm test`; validator; CI a11y gate; live verification.
- **Issue links:** Adopts review-0001 OPP-04 + OPP-07. Requirements value verified against the live Mac App Store listing (macOS 10.15+, fetched this session).
- **Rollback/containment:** label map in `lib/products.ts` + small template additions + one optional schema field; `git revert -m 1` restores.
- **Evidence after completion:** Released as v0.30.0. "macOS" badge live on detail + catalog; "Requires macOS 10.15 or later" beside the header download button; trust line present linking About/Support; `platformLabel` unit-tested; requirements pinned by content test (40 tests total); shared map feeds JSON-LD `operatingSystem`. Gates green; deploy through a11y gate; live-verified. **AUDIT-0006 + DISC-0010 + MC-0015** (all due at feature step 30) run alongside.
