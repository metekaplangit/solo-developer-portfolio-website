# Task Card — STEP-0074

- **ID:** STEP-0074
- **Type:** fix (the screenshot transcript goes)
- **One outcome:** No "Screenshot transcript" control sits under the gallery on any product page.
- **Feedback:** `2026-08-03-remove-screenshot-transcript` — "remove this screenshot transcript, no need".
- **Why now:** the owner boxed it on the live Magic Notes page and asked for it to go. It also carried the largest block of duplicated prose on the site, which is why it is worked before the spacing packets that follow.
- **The accessibility question, answered rather than assumed:** the disclosure was added in STEP-0032 as a text alternative for the gallery, and removing an accessibility affordance needs more than the owner's say-so to be safe. It is safe here: it rendered nothing but `shot.alt`, and every `<img>` in the gallery still carries that exact string as its `alt` attribute. Nothing a screen reader could reach through the transcript has been lost — the same words are still on the same images. What went was a visible duplicate of them.
- **Approach:** delete the `<details class="transcript">` block, the 36 lines of CSS that only it used, and nothing else. `shot.title` is now rendered nowhere, but the content files author it and the schema keeps it, so the field stays and its comment says why.
- **Avoid:** removing `title` from the schema or the content files. It is authored data, it costs nothing, and deleting it would make this a content change as well as a component one.
- **Acceptance criteria:** (1) no "Screenshot transcript" string in any built page. (2) Every gallery image still carries its full alt text. (3) The gallery itself — track, arrows, dots, counter — is untouched. (4) No content file changes.
- **Non-goals:** the empty band above the gallery (STEP-0075), and anything else about the gallery.
- **Files:** `src/components/ScreenshotShowcase.astro`, `docs/STATUS.md`, `docs/ROADMAP.md`, `docs/CHECKPOINTS.md`, `docs/CHANGELOG.md`.
- **Evidence method:** the built pages searched for the string, and the alt attributes counted before and after.
- **Proof classification:** machine-verifiable.
- **Required checks:** `npm run build`; `npm run check`; `npm test`; `npm run test:dist`; `python3 scripts/validate-governance.py`.
- **Rollback/containment:** `git revert -m 1`. One component; nothing else references the removed markup or styles.
- **Issue links:** none. Owner feedback, 2026-08-03.
- **Evidence after completion:** Gates: build exit 0 (10 pages), check exit 0 with 0/0/0, 111/111 unit tests, 14/14 dist tests, validator 44/44. "Screenshot transcript" occurrences in `dist`: **2 → 0**. Gallery images carrying alt text: unchanged, 6 on Magic Notes and 5 on Sole Focus.
