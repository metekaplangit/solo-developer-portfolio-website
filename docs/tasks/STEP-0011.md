# Task Card — STEP-0011

- **ID:** STEP-0011
- **Type:** feature
- **One outcome:** Install the MetKap Studio **"Studio Gem"** brand mark as the site's logo — favicon + PWA icon set, a web manifest, an OG/social image, and a visible logo mark in the nav wordmark.
- **Why now:** The user generated a website icon export and asked to use it as the main logo. The site currently has a placeholder "MK" favicon and a text-only wordmark.
- **Source (read-only):** `…/icon-generator/…/metkap-studio/exports/v0001/web/` per its `web/AGENTS.md` (favicon.svg, favicon-16/32/48, apple-touch-icon-180, icon-192/512, icon-maskable-512) + `store/…-1024.png` (OG image). **Nothing in that export is modified.**
- **Acceptance criteria:** (1) The icon set is copied into `public/` (replacing the old `favicon.svg`); `public/site.webmanifest` references the PWA icons (incl. maskable). (2) `BaseLayout` head links the svg + png favicons, apple-touch-icon, manifest, and og/twitter image. (3) The gem mark shows in the nav next to "MetKap Studio". (4) `npm run build`, `npm run check`, `npm test`, `python3 scripts/validate-governance.py` pass; browser tab shows the new gem.
- **Non-goals:** Modifying the icon-generator export; redesigning the theme; per-product icons (Sole Focus keeps its own).
- **Evidence method:** command/log artifact (build/check/test/validator; assets in `dist/`) + runtime screenshot of the nav logo + tab icon.
- **Proof classification:** machine-verifiable + manual-runtime (visual).
- **Required checks:** `npm run build`; `npm run check`; `npm test`; `python3 scripts/validate-governance.py`; runtime visual.
- **Issue links:** LEDGER-001, LEDGER-002 open (low, unrelated).
- **Rollback/containment:** work on `feature/STEP-0011-studio-gem-logo`; revert the merge commit or `git checkout -- public/`; asset/head/nav only.
- **Evidence after completion:** All merge-critical gates Pass (2026-07-02); released as v0.11.0 and live. `npm test` → 34 passed; `npm run check` → 0 errors; `npm run build` → 8 routes + the full icon set, `site.webmanifest`, and `og-image.png` in `dist/`; head links include favicon.svg + favicon-32 + apple-touch-icon + manifest + og:image; `python3 scripts/validate-governance.py` → 40/40 exit 0. Runtime: nav shows the gem logo mark next to "MetKap Studio"; no console errors. Icon-generator export v0001 left unmodified. Live URL 200 over HTTPS after deploy.
