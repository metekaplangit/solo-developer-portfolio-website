# Task Card — STEP-0038

- **ID:** STEP-0038
- **Type:** feature (UI — full-site "Graphite Refined" restructure)
- **One outcome:** All four primary pages (home, catalog, product detail, about) rebuilt on a shared band system — one idea per band, generous vertical air, prose capped at a readable measure, product imagery leading — so the site stops reading as dense text cramped into narrow columns.
- **Why now:** User, third time on the same complaint: *"we have problems with too much text cramped up into spaces"*, and, on the external `design_handoff_metkap_redesign` mockup: *"I want a complete make over with this new design… much simpler, much more effective, much more visually appealing."* STEP-0036 widened the shell and STEP-0037 aligned the buttons, but the product and about pages still render everything inside a 43rem prose column.
- **Source of truth:** `~/Downloads/design_handoff_metkap_redesign/` (README spec + `MetKap Studio.dc.html` mockup, reviewed 2026-07-18). Adopted as a **menu, not a spec** — see Deviations.
- **Acceptance criteria:** (1) A shared band/rhythm system in `global.css` (page gutter, band spacing, prose measures) used by all four pages — no per-page magic numbers. (2) Home: hero → featured → catalog → maker note, with the hero sharing ONE left rail with every band below it (fixes the measured 198px-vs-48px misalignment the critique flagged and the mockup reproduced). (3) Product detail breaks out of the 43rem column into full bands: header, action row, gallery, prose, feature grid, download panel. (4) About: values grid + contact strip, replacing the single prose column. (5) Catalog: featured row + grid, driven entirely by the content collection. (6) Every claim on screen traceable to `src/content/` or an existing page — no invented products, quotes, dates or counts. (7) The top-of-page download button survives on every relevant surface and the home hero CTA names the product. (8) No horizontal overflow at 390/430/744/820/1024/1280/1440; a11y ≥ 0.95; gates green.
- **Deviations from the handoff (deliberate):**
  - **Dropped:** the "Untitled game" / "Untitled app" placeholder cards and the "3 total · 1 released" count. The collection has one product; shipping two invented ones is fabricated proof (standing rule). Re-add as real `in-development` entries if/when they exist.
  - **Dropped:** the handoff's home maker quote (*"I build software small on purpose…"*) — first-person copy attributed to the user that exists nowhere in the repo. The home band reuses the **real** `makerNote` from `sole-focus.md`.
  - **Restored:** the hero download button the mockup removed (user instruction, STEP-0028), upgraded to name the product per the critique's P1.
  - **Fixed, not copied:** the hero's inboard left edge; and the handoff's own screenshot frames stack `border` + `box-shadow`, violating the Single-Cue Rule it states — single cue used instead.
  - **Also fixed:** `MakerNote.astro`'s `border-left: 3px` side-stripe (absolute ban + DESIGN.md don't-list).
- **Non-goals:** Palette, type stack, or webfont changes (tokens are already correct); new product content; the filter row's behaviour (visual only until there is more than one product); privacy/support/404 page redesigns beyond inherited shared rules.
- **Evidence method:** before/after screenshots per page; scripted overflow + left-rail alignment sweep at 7 widths; axe-core in-page audit; gates; live verification after deploy.
- **Proof classification:** manual-runtime (measured/visual) + machine-verifiable (build/tests/validator/a11y gate).
- **Required checks:** `npm run build`; `npm run check`; `npm test`; `python3 scripts/validate-governance.py`; CI a11y gate; live verification.
- **Issue links:** External design handoff; `/impeccable critique` snapshot `.impeccable/critique/2026-07-18T10-38-29Z__src-pages-index-astro.md` (24/40, three P1s addressed here).
- **Rollback/containment:** Presentation-only — page layouts, component styles, and `global.css` tokens. No content, schema, or data changes. `git revert -m 1` on the merge restores v0.35.1.
- **Evidence after completion:** Released as **v0.36.0**. Band system landed in `global.css`; all four pages rebuilt; PageHeader + policy/support columns moved onto the shared rail. Measured: **0 horizontal overflow across 56 page/width checks** (7 routes × 390/430/744/820/1024/1280/1440); every non-icon heading aligned to the nav rail (the two remaining offsets are the deliberate icon-beside-title compositions, a constant 88px); **0 axe-core violations across all 7 routes** (two found and fixed en route: a serious `color-contrast` on the hero CTA caused by an opacity-gated entrance animation, and `landmark-complementary-is-top-level` from MakerNote's `<aside>` nested in a named section). Gates: build 8 / check 0-0-0 / 45 tests / validator pass. No checkpoint due (feature #37).
