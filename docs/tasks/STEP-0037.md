# Task Card — STEP-0037

- **ID:** STEP-0037
- **Type:** feature (UI — alignment & polish pass)
- **One outcome:** Buttons and text read as a considered, aligned set instead of mismatched, ragged copy-paste — starting with the home featured panel and applied through shared button/typography rules that touch every page.
- **Why now:** User: the featured buttons "don't look aligned or the same size", text is "all over the place", "copy-pasted going line to line", many little polish issues; wants everything more aligned and beautifully placed, on all pages.
- **Acceptance criteria:** (1) One shared button height (48px) so any button group aligns; the download button leads via fill/width/glyph, not a taller box (measured: featured CTA was 56/49/49px → now 48/48/48). (2) Featured text column widened (0.85→1fr) so the lede wraps cleanly (4→3 lines) and the trust line fits one line; dead `.spotlight-highlights` CSS removed. (3) `FactsLine` refactored so the facts line only ever breaks *between* facts, never mid-phrase ("macOS 10.15 or / later" fixed), with graceful narrow-screen fallback. (4) No horizontal overflow on any route at 390/1280; a11y ≥ 0.95; gates green.
- **Non-goals:** New content/claims; per-page bespoke redesigns beyond the shared rules + home this packet; the deferred description refresh.
- **Evidence method:** measured button heights + facts wrapping in preview; overflow sweep across all 7 routes at 390 + 1280; before/after screenshots; gates; live curl.
- **Proof classification:** manual-runtime (measured/visual) + machine-verifiable (build/tests/validator/a11y gate).
- **Required checks:** `npm run build`; `npm run check`; `npm test`; validator; CI a11y gate; live verification.
- **Issue links:** User-directed polish pass, following the STEP-0036 redesign.
- **Rollback/containment:** CSS + one component refactor + home CSS; `git revert -m 1` restores.
- **Evidence after completion:** Released as v0.35.0. Unified 48px buttons site-wide; featured CTA aligned; lede/facts/trust wrap cleanly; overflow-free sweep on all 7 routes at 390/1280; gates green; deploy through a11y gate; live-verified. **MC-0018 + DISC-0012** (due at feature step 36) run alongside.
