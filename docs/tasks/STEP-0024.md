# Task Card — STEP-0024

- **ID:** STEP-0024
- **Type:** feature (content — apply the v2 store-listing copy pack)
- **One outcome:** Refresh the Sole Focus website copy from the reviewed v2 copy pack (competitor/ASO-grounded) and present the detail page as clear, divided, appealing sections — updating summary, features, SEO title/description, and the long description body; no false claims (never imply app/website blocking).
- **Why now:** User supplied a reviewed, truthful store-listing copy pack (`.../products/sole-focus/versions/v2/`) and asked to update the site's text descriptions and make them nicer, consistent, and divided.
- **Acceptance criteria:** (1) `summary`, `features`, `seo.title`, `seo.description`, and the Markdown body of `src/content/products/sole-focus.md` are updated to the v2 copy, adapted for web (sentence case, not App Store ALL-CAPS). (2) The detail page reads as **divided, scannable sections**: a scannable "Highlights" grid (from `features`) plus a narrative body split into titled sections (Two ways to work · Calm, and always in reach · See your progress · Private, offline, yours) and a short **FAQ**. (3) Prose gets heading/emphasis styling so the sections look intentional. (4) **Truthful** — every claim maps to the pack's facts; **no blocking claim**, privacy stated exactly (no account, no cloud, no tracking, no subscription, zero network requests). (5) `summary` stays ≤200 chars; the change propagates consistently to the home spotlight, product card, and `SoftwareApplication` JSON-LD (`featureList`, description). (6) `npm run build`, `npm run check`, `npm test`, validator all pass; runtime preview shows the divided sections with no overflow (desktop + mobile).
- **Non-goals:** Rewriting the studio-level home hero/meta (the pack's meta is Sole-Focus-specific → belongs on the product page, not the studio home); changing the privacy policy page; App Store submission (this is the website surface only); adding FAQPage JSON-LD (possible later); a schema change for feature groups (grouping lives in the Markdown body).
- **Evidence method:** command/log (build/check/test/validator) + built-HTML assertion the detail page shows the new section headings + FAQ + updated meta description/title; runtime preview (desktop + mobile) of the divided sections; grep confirming no "block"/"blocker" claim was introduced.
- **Proof classification:** machine-verifiable (built HTML + meta) + manual-runtime (visual preview).
- **Required checks:** `npm run build`; `npm run check`; `npm test`; validator; runtime preview (desktop + mobile).
- **Issue links:** applies the v2 copy pack (`store-listing-copy-studio/.../sole-focus/versions/v2/`).
- **Rollback/containment:** work on `feature/STEP-0024-listing-copy`; revert the merge or branch from `v0.23.x`; changes scoped to the Sole Focus content file + the detail page template/styles.
- **Evidence after completion:** Released as v0.24.0. `sole-focus.md` now carries
  the v2 `summary` (127/200), 7 sharper `features`, refreshed `seo.title`
  ("Sole Focus — Pomodoro & Stopwatch Timer for Mac") + `seo.description`, and a
  body split into six sections. Built HTML confirms the new `<title>`/meta
  description and section headings (Highlights, Two ways to work, Calm and always
  in reach, See your progress, Private offline yours, Common questions, Get it);
  JSON-LD `featureList` = 7. Truthfulness grep: the only "block/blocker" mention is
  the FAQ negation ("No … not a blocker") — the pack-mandated safe statement.
  Runtime preview: header summary, a check-marked Highlights grid above the
  narrative, section headings with hairline dividers (24px, 1px border-top), and
  styled FAQ bold lead-ins; **no horizontal overflow**. `npm test` → 36 passed;
  `npm run check` → 0/0/0; `npm run build` → 8 routes; validator → 40/40.
  **MC-0012 + DISC-0008** run (feature step #24). Owner-only pre-submit actions
  (trademark, live ASC counters, marketing URL, quote provenance) left open.
