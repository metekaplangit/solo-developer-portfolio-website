# Task Card — STEP-0016

- **ID:** STEP-0016
- **Type:** feature (UI — appeal & motion)
- **One outcome:** Make the site more appealing, dynamic, and engaging — in the **tasteful/premium** register — by adopting the honest, static-friendly patterns real App Store / Mac App Store creators use, without fabricating any social proof.
- **Why now:** User asked to study direct rivals' creator websites and raise UI quality (more appealing / dynamic / engaging). Chose the "tasteful & premium" direction.
- **Research basis (lean, non-agent):** Things ([culturedcode.com/things](https://culturedcode.com/things/)) and Raycast ([raycast.com](https://www.raycast.com/)): lead the hero with the product + an emotional value line; scannable feature grids over bullet lists; restrained dark theme + subtle depth + tasteful scroll motion; repeated CTAs; social proof via awards/press/avatars/metrics.
- **Honesty constraint:** Sole Focus is unreleased — **no** App Store badges, press logos, user counts, testimonials, awards, or fabricated screenshots. Adopt only what we can state truthfully now.
- **Acceptance criteria:** (1) **Home featured spotlight** — the featured product gets a richer panel (large `ProductAvatar` + name + status + emotional summary + real highlight chips + CTAs) instead of a small card. (2) **Feature grid** — the app detail Features list becomes a scannable card grid. (3) **Tasteful motion** — CSS-only scroll-reveal (`animation-timeline: view()`), guarded by `prefers-reduced-motion` **and** `@supports`, zero JS, progressive-enhancement (content fully visible without support); refined depth/hover. (4) Static/zero-JS/zero-cost preserved; dark-premium restraint kept. (5) WCAG AA contrast + focus preserved; no horizontal overflow desktop/mobile. (6) `npm run build`, `npm run check`, `npm test`, `python3 scripts/validate-governance.py` pass.
- **Deferred (needs assets):** device mockups / real screenshots (research item C), App Store download badges (needs store link), social proof (needs real proof). Kept on roadmap.
- **Non-goals:** New pages, copy invention beyond existing content, client JS, light theme.
- **Evidence method:** command/log (build/check/test/validator) + before/after screenshots (desktop + mobile) + overflow scan.
- **Proof classification:** machine-verifiable + manual-runtime (visual appeal is human-judged).
- **Required checks:** `npm run build`; `npm run check`; `npm test`; `python3 scripts/validate-governance.py`; runtime visual + overflow scan.
- **Issue links:** LEDGER-002 (a11y CI) still open — motion added here is a reason to land it.
- **Rollback/containment:** work on `feature/STEP-0016-appeal-motion`; revert the merge or branch from `v0.15.x`; component/CSS only.
- **Evidence after completion:** All merge-critical gates Pass (2026-07-03); released as v0.16.0. `npm test` → 34 passed; `npm run check` → 0/0/0; `npm run build` → 8 routes; validator → 40/40. Home now leads the Featured section with a **product spotlight** (112px `ProductAvatar` + accent-glow panel + status badges + emotional summary + 3 real highlight bullets + View/Privacy CTAs). App detail Features became a **check-marked card grid**. **Scroll-reveal** added via `.reveal` (CSS `animation-timeline: view()`, guarded by `prefers-reduced-motion` + `@supports`; verified opacity resolves to 1 in-view, so no content is hidden without support). Runtime: no overflow on any of the 8 pages at mobile 375px; spotlight avatar 112px, 3 highlights. Honesty preserved — no fabricated badges/press/testimonials/screenshots. Markdown Consistency **MC-0008** run. Deferred: real screenshots/device mockups (research item C).
