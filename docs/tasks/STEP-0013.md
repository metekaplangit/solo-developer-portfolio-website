# Task Card — STEP-0013

- **ID:** STEP-0013
- **Type:** feature
- **One outcome:** Show the product's **app icon** in each per-product privacy page header, and replace the Sole Focus icon with the **current** one shipped in the app.
- **Why now:** The user wants the individual privacy pages (e.g. Sole Focus) to carry the app's identity, and the previously installed Sole Focus icon was stale — the app now ships an updated icon (orange-rimmed clock, dark center, sepia tile).
- **Acceptance criteria:** (1) `PolicyArticle` accepts an optional `icon` (MediaAsset) and renders it beside the title as a rounded app tile; the site-wide (`global`) policy — which has no product — renders no icon and is unaffected. (2) The privacy `[slug]` route looks up the product by `productId` and passes its icon. (3) The Sole Focus icon is sourced from the read-only app export (`submission-assets/export/v0001/app/sole-focus-icon-source.svg`) — the current design — installed as `public/media/sole-focus/icon.svg`; the old flat-orange placeholder is removed. (4) Icon renders consistently on the privacy header and the home ProductCard (single source). (5) `npm run build`, `npm run check`, `npm test`, `python3 scripts/validate-governance.py` pass; layout holds on desktop + mobile.
- **Non-goals:** Modifying the read-only `PromodoApp/` source; product detail-page hero icon (separate polish); changing policy text.
- **Evidence method:** command/log artifact (build/check/test/validator) + before/after runtime screenshot of the Sole Focus privacy page and home card.
- **Proof classification:** machine-verifiable + manual-runtime (visual is human-judged; user confirmed the correct icon).
- **Required checks:** `npm run build`; `npm run check`; `npm test`; `python3 scripts/validate-governance.py`; runtime visual.
- **Issue links:** LEDGER-001, LEDGER-002 open (low, unrelated).
- **Rollback/containment:** work on `feature/STEP-0013-privacy-icon`; revert the merge commit or branch from `v0.12.x`; component/route/content/asset only.
- **Evidence after completion:** All merge-critical gates Pass (2026-07-02); released as v0.13.0. `npm test` → 34 passed; `npm run check` → 0 errors/0 warnings; `npm run build` → 8 routes; `python3 scripts/validate-governance.py` → 40/40 exit 0. Runtime: Sole Focus privacy header shows the current app icon (SVG, rounded tile + soft shadow) beside the title; global policy shows none; home ProductCard uses the same source. Verified desktop + mobile; no console errors. No scheduled checkpoint at feature step 13.
