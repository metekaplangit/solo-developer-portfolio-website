# Task Card — STEP-0010

- **ID:** STEP-0010
- **Type:** feature
- **One outcome:** Publish **Sole Focus** (the studio's first real product) on the site — accurate product entry, a review-ready privacy policy true to the app, and its real icon — and retire the three placeholder demo products so the live site is honest.
- **Why now:** The user provided the real Sole Focus codebase/product info (read-only) and asked to feature it; the site is live at metkapstudio.com, so placeholder apps should be replaced with the real one.
- **Source of truth:** `../PromodoApp/` (read-only): `README.md`, `docs/PRIVACY.md`, `appstore/REVIEW_NOTES.md`, `app-icon.svg`, `src-tauri/tauri.conf.json` (name "Sole Focus", v1.27.3, id com.metkapstudio.solefocus). **Nothing in that folder is modified.**
- **Acceptance criteria:** (1) `src/content/products/sole-focus.md` — type app, status in-development, real features/platform (macOS), no store links yet (unpublished), links to its policy + support, real icon. (2) `src/content/policies/sole-focus.md` — accurate to `docs/PRIVACY.md` (collects nothing, local-first storage, erase path, optional permissions), with the required `retention`/deletion statement and `support@metkapstudio.com`. (3) Real icon copied to `public/media/sole-focus/icon.svg`; `ProductCard`/detail render the icon when present, else the monogram. (4) The three placeholder products + their per-product policies removed; global policy kept. (5) `npm run build`, `npm run check`, `npm test`, `python3 scripts/validate-governance.py` pass.
- **Non-goals:** Modifying the PromodoApp repo; real screenshots (none yet — in development); changing the studio favicon; store links (not published).
- **Evidence method:** command/log artifact (build/check/test/validator) + runtime screenshots of the Sole Focus card + detail + privacy page.
- **Proof classification:** machine-verifiable + manual-runtime (visual).
- **Required checks:** `npm run build`; `npm run check`; `npm test`; `python3 scripts/validate-governance.py`; runtime visual.
- **Issue links:** LEDGER-001 (monogram dup — icon rendering touches both card + detail; may extract), LEDGER-002 (a11y CI) open.
- **Rollback/containment:** work on `feature/STEP-0010-sole-focus`; revert the merge commit or branch from `v0.9.0`; content/asset/CSS only; removed placeholders are recoverable from Git history.
- **Evidence after completion:** All merge-critical gates Pass (2026-07-02); released as v0.10.0 and live. `npm test` → 34 passed; `npm run check` → 0 errors (30 files); `npm run build` → 8 routes (home, /apps/, /apps/sole-focus/, /privacy/, /privacy/sole-focus/, support, about, 404) + sitemap/robots/favicon; icon copied to `dist/media/sole-focus/icon.svg`; `python3 scripts/validate-governance.py` → 40/40 exit 0. Runtime: Sole Focus detail + home card render the real orange icon, honest "In development / Not yet available", all 7 features, accurate privacy page; no console errors. Placeholders removed (build dropped from 11→8 routes). Audit AUDIT-0002 + Markdown Consistency MC-0005 run. Live URL 200 over HTTPS after deploy.
