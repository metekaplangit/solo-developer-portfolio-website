# Task Card — STEP-0040

- **ID:** STEP-0040
- **Type:** feature (UI — critique follow-ups)
- **One outcome:** The mechanical defects surfaced by the post-redesign `/impeccable critique` are fixed — every mobile visitor can see every destination, the facts line never ends a wrapped row on a stray separator, and the maker's note is one component instead of two divergent treatments.
- **Why now:** Second critique of the home page (25/40, up from 24/40) confirmed the v0.36/v0.37 restructure solved layout but surfaced defects underneath it. Owner reviewed the findings and elected to KEEP the headline and the App Store lead image; Q3 (which remaining findings to fix) was answered "no preference", so scope was set by judgement: mechanical defects yes, hero restructure no.
- **Acceptance criteria:** (1) At 390/430px every nav destination is visible without scrolling; header height does not regress past 94px; no document overflow at any width **including 320px**. (2) `FactsLine` separators lead their fact, so a wrapped line never ends on a middot. (3) One `MakerNote` component serves both home (`tone="lead"`, visible heading, reply promise on its own line) and product page (`tone="inline"`). (4) One accent-filled primary per page. (5) Hero `sizes` matches the measured slot; srcset reaches 1600w. (6) 0 axe violations across 8 routes; gates green.
- **Deliberately NOT done (owner-directed):** the [P0] hero-names-the-product restructure (taste call on a layout the owner has just approved — flagged, not actioned), the headline, and the App Store lead image. The first two are recorded in `.impeccable/critique/ignore.md` so future critiques stop re-raising them. **The lead image's alt text gap is NOT ignored** — it omits the marketing copy baked into the image (WCAG 1.1.1) and remains open.
- **Non-goals:** Hero copy or structure; screenshot assets; new content.
- **Evidence method:** scripted nav/overflow probes at 320–1440; axe-core per route; gates; live verification.
- **Proof classification:** manual-runtime (measured) + machine-verifiable (build/tests/validator/a11y gate).
- **Required checks:** `npm run build`; `npm run check`; `npm test`; validator; CI a11y gate; live verification.
- **Issue links:** `.impeccable/critique/2026-07-18T11-48-04Z__src-pages-index-astro.md` (25/40, trend 24 → 25).
- **Rollback/containment:** Presentation only. `git revert -m 1` restores v0.37.0.
- **Evidence after completion:** Released as **v0.37.1**. Mobile nav: "Home" hidden ≤40rem (the brand lockup already links home), leaving four destinations on one visible row — measured 0px hidden at 390/430/640, header steady at 94px, and `overflow-x` retained purely as a safety valve below ~360px after a nowrap-only attempt pushed the **document** 39px wide at 320px. A two-row wrap was tried and rejected: with the `pointer: coarse` 44px rule it cost ~144px of header, the very height the original scroll fix existed to avoid. **64 geometry checks (320–1440 × 8 routes): 0 overflow. 0 axe-core violations across all 8 routes.** Gates: build 8 / check 0-0-0 / 45 tests / validator pass. No checkpoint due (feature #39).
