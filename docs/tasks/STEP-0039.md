# Task Card — STEP-0039

- **ID:** STEP-0039
- **Type:** feature (UI — copy shortening + policy/support pages onto the band system)
- **One outcome:** Download buttons carry short labels, the studio headline stops claiming a single platform, and the privacy, support and 404 pages are rebuilt on the same band system as the rest of the site — so no surface is left on the old layout.
- **Why now:** User-directed, four asks: (1) *"drop these very long button names. Just say Download"*; (2) *"the first download button on the home page to change to Mac App Store"*; (3) *"I'm not just developing for the Mac. Just change the main name to Brilliant Products for All"*; (4) *"make sure our privacy and support web pages also align with the rest of the application"*.
- **Acceptance criteria:** (1) `DownloadButton` renders "Download" by default everywhere; the home hero renders "Mac App Store"; no long label survives in the build. (2) Accessible names are unchanged — `aria-label` still reads "Download <product> on the <store>" — and every visible label remains a substring of it (WCAG 2.5.3 Label in Name). (3) `site.headline` is "Brilliant Products for All"; no studio-level copy claims Mac-only; per-product platform requirements are untouched. (4) Support, privacy overview, per-product policy and 404 all use the band system, shared left rail, and the site's panel/card treatment. (5) 0 horizontal overflow and 0 heading misalignment across 8 routes × 7 widths; 0 axe violations; gates green.
- **Non-goals:** New policy or support *content* (presentation only — every sentence is the existing text or a rewrap of it); per-product platform claims; palette/type changes.
- **Evidence method:** built-output label grep; scripted overflow + left-rail sweep at 8 routes × 7 widths; axe-core in-page audit per route; screenshots; gates; live verification.
- **Proof classification:** manual-runtime (measured/visual) + machine-verifiable (build/tests/validator/a11y gate).
- **Required checks:** `npm run build`; `npm run check`; `npm test`; `python3 scripts/validate-governance.py`; CI a11y gate; live verification.
- **Issue links:** Follows STEP-0038 (v0.36.0) full-site restructure.
- **Rollback/containment:** Presentation + copy only; no schema or product data changes. `git revert -m 1` on the merge restores v0.36.0.
- **Evidence after completion:** Released as **v0.37.0**. Built output shows exactly three download buttons: "Mac App Store" (home hero) and "Download" ×2 (catalog, product page), with the full accessible name preserved on each. Headline live as "Brilliant Products for All"; `PRODUCT.md` positioning updated to platform-neutral so docs and site agree. Support rebuilt as hero + contact panel + "What to include" cards + supported products; privacy policy sections rebuilt as a panel grid; privacy index list rebuilt as cards; 404 moved onto the rail. Measured: **56 geometry checks — 0 overflow, 0 misaligned headings** (404 was the last off-rail surface); **0 axe-core violations across all 8 routes**. Gates: build 8 / check 0-0-0 / 45 tests / validator pass. No checkpoint due (feature #38).
