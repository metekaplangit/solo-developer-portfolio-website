# Task Card — STEP-0015

- **ID:** STEP-0015
- **Type:** feature (UI polish — research-driven)
- **One outcome:** Apply the top research-backed UI improvements — fix a real WCAG contrast fail, tighten the reading measure, unify the product identity into one shared component, and reduce visual noise — raising quality and accessibility without changing content.
- **Why now:** User asked to research best-in-class comparable sites and apply the findings. Research (salvaged from the deep-research run + a local contrast audit) surfaced concrete, cited fixes.
- **Acceptance criteria:** (1) **A — contrast:** `--faint` clears WCAG AA 4.5:1 on `--bg` and card surfaces at small sizes (was #71757e = 3.9:1). (2) **B — measure:** prose column tightened toward the ~45–90ch comfortable range (Butterick) via `--maxw-prose`. (3) **#3 — ProductAvatar:** one shared `ProductAvatar` component (icon-or-monogram) used by ProductCard, app detail, and privacy header; the same product looks identical everywhere; **resolves LEDGER-001** (monogram/icon duplication). (4) **D — fewer borders:** elevated cards (ProductCard, detail header) drop the stacked border, keeping one separation cue (surface + shadow) per Refactoring UI. (5) **E — focus:** no focus suppression anywhere; a consistent visible `:focus-visible` ring. (6) No horizontal overflow desktop/mobile. (7) `npm run build`, `npm run check`, `npm test`, `python3 scripts/validate-governance.py` pass.
- **Deferred (needs assets):** **C — hero/detail product screenshot imagery** — highest visual-impact lever, blocked until real Sole Focus screenshots exist. Kept on the roadmap.
- **Non-goals:** New pages/sections, copy rewrites, light theme, client JS, real screenshot pipeline.
- **Evidence method:** command/log (build/check/test/validator) + scripted contrast calc + runtime avatar-size/overflow scan across all pages + screenshots.
- **Proof classification:** machine-verifiable + manual-runtime.
- **Required checks:** `npm run build`; `npm run check`; `npm test`; `python3 scripts/validate-governance.py`; runtime visual + overflow/contrast scan.
- **Issue links:** **Resolves LEDGER-001**; LEDGER-002 (a11y CI) still open.
- **Rollback/containment:** work on `feature/STEP-0015-ui-research-polish`; revert the merge or branch from `v0.14.x`; token + component/CSS only.
- **Evidence after completion:** All merge-critical gates Pass (2026-07-03); released as v0.15.0. `npm test` → 34 passed; `npm run check` → 0/0/0; `npm run build` → 8 routes; validator → 40/40 exit 0. Contrast: `--faint` #8b909a → 5.59:1 on bg (4.72–5.09:1 on surfaces). Runtime scan (mobile 375px, all 8 pages): no overflow; ProductAvatar 48px/r14 on cards, 72px/r20 on detail + privacy (identical). LEDGER-001 resolved (monogram logic centralised in `ProductAvatar.astro`; removed from ProductCard + detail). C deferred (screenshots).
