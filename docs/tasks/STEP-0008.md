# Task Card — STEP-0008

- **ID:** STEP-0008
- **Type:** feature
- **One outcome:** Switch the live site to the purchased custom domain **metkapstudio.com** — served at the domain root over HTTPS via GitHub Pages + Cloudflare DNS.
- **Why now:** The user purchased metkapstudio.com (Cloudflare Registrar) and authorized full setup. This is the deferred custom-domain step.
- **Acceptance criteria:** (1) `astro.config.mjs` sets `site: 'https://metkapstudio.com'`, `base: '/'`; `withBase()` becomes a no-op and all links resolve at root. (2) `public/CNAME` = `metkapstudio.com`; `robots.txt`/sitemap point at the domain. (3) Support email = `support@metkapstudio.com`. (4) GitHub Pages custom domain set to metkapstudio.com (via API/CNAME). (5) `npm run build`, `npm run check`, `npm test`, and `python3 scripts/validate-governance.py` all pass. (6) **Blocked-pending-user:** Cloudflare DNS records added + HTTPS provisioned → metkapstudio.com serves 200 over HTTPS (verified once DNS propagates).
- **Non-goals:** Content changes; UI redesign; email mailbox hosting (Cloudflare Email Routing is a documented user step); Cloudflare proxy/CDN tuning beyond the recommended DNS-only setup.
- **Evidence method:** command/log artifact (build at root; Pages API cname set) + Blocked reason for the DNS-dependent live-HTTPS check with exact manual steps.
- **Proof classification:** machine-verifiable for code/config; **needs-human-runtime / blocked** for the live-domain HTTPS check (depends on user-side DNS + GitHub cert provisioning, up to 24h).
- **Required checks:** `npm run build`; `npm run check`; `npm test`; `python3 scripts/validate-governance.py`; live-domain smoke (Blocked until DNS).
- **Issue links:** LEDGER-001, LEDGER-002 open (low, unrelated).
- **Rollback/containment:** work on `feature/STEP-0008-custom-domain`; revert the merge commit or branch from `v0.7.0`; to fully revert, restore `base`/`site` to the project-site values and remove `public/CNAME`. Non-destructive; DNS is user-controlled at Cloudflare.
- **Evidence after completion:** Code/GitHub side Pass (2026-07-02); released as v0.8.0. `npm test` → 33 passed; `npm run check` → 0/0/0; `npm run build` → 11 routes with **root-absolute links** (no sub-path; grep confirms 0 `solo-developer-portfolio-website` refs); `dist/CNAME` = metkapstudio.com; canonical = `https://metkapstudio.com/`; robots sitemap on the domain; `python3 scripts/validate-governance.py` → 40/40 exit 0. GitHub Pages `cname` set to metkapstudio.com (`https_enforced: false` until cert provisions). **Live-HTTPS on metkapstudio.com is `Blocked`** pending user-side Cloudflare DNS (A/AAAA apex + `www` CNAME, DNS-only) + GitHub certificate — exact records/steps in `DEPLOYMENT.md`. MC-0004 run.
