# Task Card — STEP-0059

- **ID:** STEP-0059
- **Type:** fix (content integrity / edge rewriting)
- **One outcome:** `support@metkapstudio.com` reaches the visitor as readable
  text in the HTML the live site serves, with no JavaScript required.
- **Why now:** AUDIT-0009 Finding SMOOTH-0009-1. Measured 2026-08-01:
  `curl -s https://metkapstudio.com/support/ | grep -c support@metkapstudio.com`
  returned **0**, while the same page in `dist` contained the address twice.
  Cloudflare's Email Address Obfuscation rewrites it at the edge into
  `<span class="__cf_email__">` plus `/cdn-cgi/l/email-protection#33404643…`,
  decoded only by an injected `email-decode.min.js`. With JavaScript off or
  blocked, the site's only support contact did not render at all — on a site
  whose support page promises "a real person answers".
- **Prior art:** Cloudflare documents a per-element opt-out —
  `<!--email_off-->address<!--/email_off-->` — alongside the zone-wide dashboard
  toggle (Security Settings / Scrape Shield → Email Address Obfuscation) and the
  API `PATCH email_obfuscation: "off"`. (Cloudflare WAF docs, *Email Address
  Obfuscation*; checked 2026-08-01.)
- **Avoid:** the dashboard toggle alone. It fixes the symptom but lives outside
  this repository, cannot be proved by any check the project owns, and silently
  regresses if the zone is ever reconfigured. The in-repo markers are inert
  comments when the feature is off, so they compose with the toggle rather than
  competing with it.
- **Approach:** one component, `src/components/SupportEmail.astro`, wrapping a
  **slot** in the marker pair. Every place the address reaches HTML now goes
  through it.

## Two things the implementation had to get right

**The wrapper must not render the link itself.** The first version took `label`
and `href` props and emitted the `<a>` via `set:html`. That `<a>` carried no
`data-astro-cid-*` scope attribute, so page-scoped rules stopped matching it —
caught on the About page, where `.elsewhere a { color: var(--muted) }` no longer
applied and the link rendered white against muted siblings. Measured:
`rgb(255,255,255)` where its siblings were `rgb(162,167,179)`. Wrapping a slot
keeps the `<a>` in the caller's file, and its scope with it.

**JSON-LD needs no wrapper.** The live home page serves
`"email":"support@metkapstudio.com"` inside `application/ld+json` intact — the
edge leaves it alone. `src/lib/schema.ts` is therefore unchanged, and the one
remaining unwrapped occurrence in `dist/index.html` is that entry, by design.

- **Files:** `src/components/SupportEmail.astro` (new);
  `src/pages/index.astro`, `src/pages/about/index.astro`,
  `src/pages/support/index.astro` (2 sites),
  `src/components/PolicyArticle.astro` (4 links + the retention prose).
  Seven call sites. `src/lib/site.ts` and `src/lib/schema.ts` untouched.
- **The retention statement** (`src/content/policies/global.md`) is free prose
  that names the address and is rendered as a text node, so it is wrapped as a
  whole paragraph rather than per-address.

- **Acceptance criteria:** (1) every rendered occurrence of the address in
  `dist` sits inside an `<!--email_off-->…<!--/email_off-->` pair — the JSON-LD
  entry excepted, proven safe against the live site. (2) All 8 built pages are
  byte-identical to `main` once the markers are stripped. (3) The About-page
  link colour matches its siblings again. (4) After deploy, the live
  `/support/`, `/about/`, `/privacy/` and `/` pages each serve the address as
  text and carry zero `__cf_email__` spans.
- **Non-goals:** the Cloudflare Web Analytics beacon (AUDIT-0009
  SMOOTH-0009-2 — needs the account holder); changing any Cloudflare dashboard
  setting; wrapping the JSON-LD address; inlining the render-blocking
  stylesheets (measured, below the impact bar).
- **Evidence method:** a structural scan of `dist` that pairs each address
  against the marker spans around it; a byte-diff of all 8 built pages against a
  `main` worktree with the markers stripped; computed `color` read from the
  About page in a headless browser; and `curl … | grep -c` against the live site
  after deploy. All exit-code checkable; no screen required.
- **Proof classification:** machine-verifiable throughout, including the live
  assertion.
- **Rollback/containment:** one new component and seven call sites;
  `git revert -m 1`.

## Acceptance

| Check | Result |
|---|---|
| `npm run build` | exit 0 |
| `npm run check` | exit 0, 0-0-0 |
| `npm test` | exit 0, 61/61 |
| `python3 scripts/validate-governance.py` | exit 0, 44/44 |
| Every rendered address wrapped in `dist` | 10 of 10; the one bare occurrence is the JSON-LD entry, proven safe against the live site |
| No visual change | all **8** built pages byte-identical to `main` once the markers are stripped |
| About-page link colour restored | `rgb(162,167,179)`, matching its siblings |

**Still open after deploy** — the only assertion this card cannot make locally:

```
curl -s https://metkapstudio.com/support/ | grep -c support@metkapstudio.com
```

must return ≥ 1. Run it once the deploy lands.

- **Required checks:** `npm run build`; `npm run check`; `npm test`; validator;
  deploy-time Lighthouse a11y gate (≥0.95).
- **Evidence after completion:** build exit 0; check 0 errors / 0 warnings /
  0 hints; 61/61 tests; validator 44/44. Ten of ten rendered addresses wrapped
  across five pages, the single bare occurrence being the JSON-LD entry. All 8
  built pages byte-identical to `main` once the markers are stripped, so nothing
  visual changed. About-page link back to `rgb(162,167,179)`, matching its
  siblings. **Live after deploy:** `/support/` 2, `/about/` 2, `/privacy/` 5,
  `/privacy/sole-focus/` 4, `/` 2 occurrences of the address as text, and **zero
  `__cf_email__` spans on any page**.
- **Disposition:** shipped. Does not depend on any Cloudflare dashboard change,
  and stays correct if one is made later.
- **Issue links:** AUDIT-0009 (`docs/audits/AUDIT_AUDIT-0009.md`), Finding
  SMOOTH-0009-1. Finding SMOOTH-0009-2 (the analytics beacon) is **not** covered
  here — it needs an owner decision and dashboard access. Follows STEP-0058.
