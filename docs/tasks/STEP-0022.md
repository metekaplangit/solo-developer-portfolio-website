# Task Card — STEP-0022

- **ID:** STEP-0022
- **Type:** feature (SEO/AEO — structured data / JSON-LD)
- **One outcome:** Add Schema.org JSON-LD structured data across the site so search engines and answer engines can parse the studio, the catalog, and each product — the one gap found in the SEO/AEO audit (checks 1,3,4,5,6 already PASS; check 2 "structured data" was the only FAIL).
- **Why now:** User asked, acting as a senior SEO/AEO engineer, to audit SEO and "implement all fixes immediately — no approval needed," keeping fixes lean and Astro 7-compatible with no visual/app-content changes.
- **Acceptance criteria:** (1) A reusable, injection-safe `JsonLd.astro` component escapes `<` and emits `<script type="application/ld+json" is:inline>`. (2) Pure builder module `src/lib/schema.ts` produces **truthful** graphs only — no fabricated `offers`, `price`, or `aggregateRating` for unreleased apps. (3) Home emits `Organization` + `WebSite`; `/apps/` emits `BreadcrumbList` + `ItemList`; each product page emits `SoftwareApplication`|`VideoGame` (by type) + `BreadcrumbList`. (4) `BaseLayout` gains an optional `schema` prop rendered in `<head>`. (5) Every emitted block is **valid JSON** and uses absolute `https://metkapstudio.com` URLs. (6) **Zero client JS still shipped** (JSON-LD is inert data, not executable/render-blocking). (7) `npm run build`, `npm run check`, `npm test`, validator all pass.
- **Non-goals:** Changing visual design or app copy; adding ratings/reviews/offers we don't truthfully have; FAQ/HowTo schema (no such content yet); any runtime/third-party SEO service.
- **Evidence method:** command/log (build/check/test/validator) + built-HTML assertion that each page's `application/ld+json` parses as JSON and carries the expected `@type`s, plus a grep proving no `offers`/`aggregateRating`/`price` were fabricated.
- **Proof classification:** machine-verifiable (JSON parse + `@type` assertion on the built `dist/**` HTML).
- **Required checks:** `npm run build`; `npm run check`; `npm test`; validator.
- **Issue links:** closes the SEO/AEO audit "structured data" finding.
- **Rollback/containment:** work on `feature/STEP-0022-structured-data`; revert the merge or branch from `v0.21.x`; new files (`schema.ts`, `JsonLd.astro`) + a `schema` prop wired through `BaseLayout` and three page frontmatters only — no shared render path changed.
- **Evidence after completion:** Released as v0.22.0. Built HTML parsed clean: home → `["Organization","WebSite"]`; `/apps/` → `["BreadcrumbList","ItemList"]`; `/apps/sole-focus/` → `["SoftwareApplication","BreadcrumbList"]`. Honesty grep: **no** `offers`/`aggregateRating`/`price`/`ratingValue` anywhere. Perf unchanged — **0 JS files** in `dist/` before and after; detail HTML 12,707 → 14,053 bytes (+1.3 KB inert inline JSON-LD, non-render-blocking). `npm test` → 34 passed; `npm run check` → 0/0/0; `npm run build` → 8 routes; validator → 40/40. Markdown Consistency **MC-0011** run (feature step #22).
