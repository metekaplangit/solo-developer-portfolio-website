# Task Card — STEP-0012

- **ID:** STEP-0012
- **Type:** feature
- **One outcome:** Make privacy pages **easy to scan** — an "at a glance" summary, strictly separated sections each with its own headline and bullet points, and no redundant wall-of-text prose — while keeping full Apple 5.1.1(i) coverage.
- **Why now:** The user finds the current privacy page complex/boring: it repeats itself (a prose narrative *and* a dense facts list) crammed into one box, with a long retention paragraph.
- **Acceptance criteria:** (1) `PolicyArticle` shows a derived at-a-glance chip strip (data / account / third-party status), then clearly separated sections — What we collect · How we use it (only when data is collected) · What we never collect · Third-party sharing · Keeping & deleting · (Your account, if any) · Contact — each with a headline + bullets/short text and visible separation. (2) The long "retention" paragraph is split: a `storedLocally[]` bullet list + a short deletion line. (3) Policy content bodies shortened to a one-line summary (no duplication). (4) Still covers Apple 5.1.1(i): collection, use, third-party equal-protection + no-AI, retention/deletion + how to request. (5) `npm run build`, `npm run check`, `npm test`, `python3 scripts/validate-governance.py` pass; a11y (semantic headings) + contrast preserved.
- **Non-goals:** Changing what data the apps actually collect; new pages; per-product legal review (developer's job).
- **Evidence method:** command/log artifact (build/check/test/validator) + before/after runtime screenshot of the Sole Focus privacy page.
- **Proof classification:** machine-verifiable + manual-runtime (visual/readability is human-judged).
- **Required checks:** `npm run build`; `npm run check`; `npm test`; `python3 scripts/validate-governance.py`; runtime visual.
- **Issue links:** LEDGER-001, LEDGER-002 open (low, unrelated).
- **Rollback/containment:** work on `feature/STEP-0012-privacy-readability`; revert the merge commit or branch from `v0.11.x`; component/content/schema only.
- **Evidence after completion:** All merge-critical gates Pass (2026-07-02); released as v0.12.0 and live. `npm test` → 34 passed; `npm run check` → 0 errors; `npm run build` → 8 routes; `python3 scripts/validate-governance.py` → 40/40 exit 0. Runtime: Sole Focus + global privacy pages verified — at-a-glance chips, separated `<h2>` sections (What we collect / never collect / third-party / keeping & deleting / permissions / contact), bullets, no redundant prose; no console errors. Apple 5.1.1(i) coverage preserved. Discussion DISC-0004 + Markdown Consistency MC-0006 run. Live URL 200 after deploy.
