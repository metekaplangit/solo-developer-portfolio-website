# Task Card — STEP-0027

- **ID:** STEP-0027
- **Type:** feature (release pass — Sole Focus live on the Mac App Store)
- **One outcome:** Reflect Sole Focus's real Mac App Store release across the site: working download button(s) with the verified store URL, "Released" status everywhere, truthful `offers` JSON-LD, and no remaining "coming soon" copy.
- **Why now:** The app is LIVE: https://apps.apple.com/us/app/sole-focus-pomodoro-timer/id6788789811?mt=12 — verified via the live listing ("Sole Focus: Pomodoro Timer", seller Mete Kaplan, Mac, Free, Productivity). The site still says "in development / coming soon" with no store link — the exact gap the deferred "release pass" was scoped for.
- **Acceptance criteria:** (1) Product content carries the verified Mac App Store link (`storeLinks: mac-app-store, available`) and `status: released`; the detail page "Get it" section renders a primary **Mac App Store** download button linking to it. (2) Status badges flip to "Released" on the detail page, catalog card, and home spotlight (all data-driven from the one content file). (3) The stale "_Currently in development — coming soon…_" body line is removed. (4) JSON-LD gains a truthful `offers` block (price 0 USD, the store URL, InStock) only for released products with a visible store link — per the schema.ts release-pass note. (5) `releaseDate`/`lastUpdated` set (2026-07-15). (6) Tests updated to assert the release state (store link present + released status). (7) All gates green; deploy passes the a11y gate; live page verified serving the button + correct link.
- **Non-goals:** Redesigning the description (open item, prose-preserving approach pending); ratings/aggregateRating JSON-LD (no rating data yet — would be fabrication); press kit; announcing elsewhere.
- **Evidence method:** command/log (build/check/test/validator) + live-listing verification of the URL + rendered page check (button present, href exact) + post-deploy live-site curl for the store URL.
- **Proof classification:** machine-verifiable (build/tests/validator/CI a11y gate) + manual-runtime (rendered/live checks).
- **Required checks:** `npm run build`; `npm run check`; `npm test`; validator; CI Lighthouse a11y gate; live verification.
- **Issue links:** Clears the long-standing STATUS ship-blocker "add the Mac App Store link once published"; completes the schema.ts deferred release pass; AUDIT-0005-F2 owner pre-submit items implicitly closed by the app passing App Review and going live.
- **Rollback/containment:** content + one schema builder + docs; `git revert -m 1` the release merge restores the pre-release site; no component markup changes.
- **Evidence after completion:** Released as **v0.27.0**. Store URL verified against the live App Store listing (right app, right seller). Detail page renders "Mac App Store" as the primary Get-it button with the exact URL; badges read "Released"; `offers` (price 0 USD, InStock, store URL) present in JSON-LD; "coming soon" line gone. Tests **38 passed** (2 new release-state assertions). Gates: build 8 routes / check 0-0-0 / validator 43/43. Deploy green through the a11y gate; live page at metkapstudio.com/apps/sole-focus/ verified serving the App Store link. **DISC-0009** (Discussion due at feature step 27) run alongside.
