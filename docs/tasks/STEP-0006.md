# Task Card — STEP-0006

- **ID:** STEP-0006
- **Type:** feature
- **One outcome:** Rebrand the site to **MetKap Studio** — the wordmark, page titles, hero, footer, and SEO use "MetKap Studio"; the About page attributes the studio to the person (Mete Kaplan).
- **Why now:** User decision: the main brand name is "MetKap Studio" (future domain metkapstudio.com). Applying it now keeps the identity consistent before further UI polish. Inserted ahead of the theme rollout (which shifts to STEP-0007; reason recorded in ROADMAP).
- **Acceptance criteria:** (1) `src/lib/site.ts` sets `name`/`brand` to "MetKap Studio" and adds a `person` field. (2) Nav wordmark, page `<title>`, hero, footer, and SEO/OpenGraph render "MetKap Studio"; no stray personal name in brand slots. (3) About page reads correctly ("MetKap Studio is the one-person studio of Mete Kaplan"). (4) `npm run build`, `npm run check`, `npm test`, and `python3 scripts/validate-governance.py` all pass.
- **Non-goals:** Purchasing/wiring the real domain (deferred custom-domain step); logo/graphics beyond the existing "MK" favicon; theme rollout to other pages (STEP-0007); changing the support email (stays placeholder until the domain is live).
- **Evidence method:** command/log artifact (build shows "MetKap Studio" in title/h1; grep confirms personal name only on About) + runtime screenshot of the home page.
- **Proof classification:** machine-verifiable.
- **Required checks:** `npm run build`; `npm run check`; `npm test`; `python3 scripts/validate-governance.py`; runtime visual.
- **Issue links:** LEDGER-001, LEDGER-002 remain open (low, unrelated).
- **Rollback/containment:** work on `feature/STEP-0006-rebrand-metkap-studio`; revert the merge commit or branch from `v0.5.0`; content-only, no data/logic risk.
- **Evidence after completion:** All merge-critical gates Pass (2026-07-02); released as v0.6.0 and live. `npm test` → 33 passed; `npm run check` → 0/0/0; `npm run build` → 11 routes, home `<title>` and `<h1>` = "MetKap Studio", "Mete Kaplan" only in `about/index.html`; validator → 40/40 exit 0. Home screenshot shows the rebrand end-to-end (wordmark, hero, lede, footer). Checkpoints MC-0003 + DISC-0002 run (DISC-0002-F1 fixed: About meta). Live URL 200 after deploy.
