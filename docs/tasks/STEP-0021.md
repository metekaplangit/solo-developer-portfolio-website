# Task Card — STEP-0021

- **ID:** STEP-0021
- **Type:** feature (code-review remediation — a11y + cleanup)
- **One outcome:** Resolve the two `/code-review` findings on STEP-0020: the redundant duplicate icon/title link, and the duplicated avatar markup — without regressing the requested "clickable icon" behaviour.
- **Why now:** High-effort code review (single-threaded, per the no-multi-agent instruction) surfaced both; user asked to work on the findings.
- **Acceptance criteria:** (1) `ProductAvatar` renders the inner avatar (img/monogram) **once**, wrapped conditionally by a dynamic `<Wrapper>` (`a` when linked, `Fragment` otherwise) — no more duplicated markup across branches. (2) New `redundant` prop: when the icon link duplicates an adjacent link (card title, spotlight name/CTA), the link is `tabindex="-1"` + `aria-hidden="true"` — still **mouse-clickable** (user's ask preserved) but no longer a duplicate keyboard tab stop or screen-reader link. (3) ProductCard + home spotlight pass `redundant`; the **privacy** icon (a unique link) stays a primary, focusable, labelled link. (4) The app **detail** icon stays unlinked (bare `<img>`). (5) The redundant pattern **passes the Lighthouse a11y gate** (verified in real CI on the PR before merge). (6) `npm run build`, `npm run check`, `npm test`, validator pass; no overflow.
- **Non-goals:** Redesigning cards into a single block-link; removing the icon link entirely.
- **Evidence method:** command/log (build/check/test/validator) + built-HTML assertion of the differing markup (card = tabindex/aria-hidden; privacy = aria-label; detail = bare img) + **CI `accessibility` job green on PR #2**.
- **Proof classification:** machine-verifiable (the CI a11y job is the proof for the a11y-sensitive change).
- **Required checks:** `npm run build`; `npm run check`; `npm test`; validator; **Lighthouse a11y job green in CI**.
- **Issue links:** resolves the two STEP-0020 review findings.
- **Rollback/containment:** work on `feature/STEP-0021-review-fixes`; revert the merge or branch from `v0.20.x`; component/caller only.
- **Evidence after completion:** Released as v0.21.0. **CI `accessibility` job GREEN on PR #2 (run 28667347857)** — the `tabindex=-1` + `aria-hidden` redundant-link pattern passes axe. `npm test` → 34 passed; `npm run check` → 0/0/0; `npm run build` → 8 routes; validator → 40/40. Built HTML: card icon `tabindex="-1" aria-hidden="true"`; privacy icon `aria-label="Open Sole Focus"` (no aria-hidden); detail 0 link elements (bare img). Avatar markup de-duplicated (single inner, dynamic `<Wrapper>`). Discussion DISC-0007 + Enhancement ENH-0003 run (feature step #21).
