# Task Card — STEP-0007

- **ID:** STEP-0007
- **Type:** feature
- **One outcome:** Apply the dark-premium design system's bespoke polish to the remaining pages (catalog, product detail, privacy global + per-product, support, about, 404) so the whole site matches the home's quality.
- **Why now:** STEP-0005 established the language and applied it to the shell + home; these pages inherit the tokens but still use plain layouts. This completes the visual pass across the site.
- **Acceptance criteria:** (1) Consistent page headers, spacing rhythm, and depth across all listed pages using the shared tokens/components. (2) CTAs use the shared `.btn` styles (support email, store links); detail page screenshots/meta refined. (3) Privacy article and support/about read cleanly with the new theme. (4) Accessibility preserved (semantic headings, focus, contrast AA, reduced-motion); responsive (mobile checked). (5) `npm run build`, `npm run check`, `npm test`, and `python3 scripts/validate-governance.py` all pass; **no product/data/logic changes** (UI-only).
- **Non-goals:** Content changes; real image assets; new pages; a light theme; the custom domain.
- **Evidence method:** command/log artifact (build/check/test/validator) + runtime screenshots (detail + one policy page, desktop + mobile spot-check).
- **Proof classification:** machine-verifiable (build/tests/validator) + manual-runtime (visual polish is human-judged).
- **Required checks:** `npm run build`; `npm run check`; `npm test`; `python3 scripts/validate-governance.py`; runtime visual.
- **Issue links:** LEDGER-001 (monogram dup — may be extracted here if a shared header component absorbs it), LEDGER-002 (a11y automation, advisory).
- **Rollback/containment:** work on `feature/STEP-0007-theme-rollout`; revert the merge commit or branch from `v0.6.0`; CSS/markup only, no data/logic risk.
- **Evidence after completion:** All merge-critical gates Pass (2026-07-02); released as v0.7.0 and live. `npm test` → 33 passed; `npm run check` → 0/0/0; `npm run build` → 11 routes; `python3 scripts/validate-governance.py` → 40/40 exit 0. Built-output checks: privacy panel present, detail store buttons use `btn-primary`, support email is a primary button, `PageHeader` on catalog/about/support (3/3), brand consistent across pages (3/3). Runtime: detail page screenshot shows elevated header panel + button CTAs + gradient screenshot frame; home re-verified; no console errors. Enhancement checkpoint ENH-0001 run. Live URL 200 after deploy.
