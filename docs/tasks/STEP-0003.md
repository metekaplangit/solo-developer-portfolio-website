# Task Card — STEP-0003

- **ID:** STEP-0003
- **Type:** feature
- **One outcome:** Core site-completeness essentials so the site is a complete, crawlable, professional static shell independent of product content: a custom 404 page, `robots.txt`, an auto-generated sitemap, and a favicon.
- **Why now:** The informational pages exist; the remaining core gap is site-level infrastructure a real public/store-reviewed site needs before deployment (M4). This is product-independent per the user's steer to finish the core and defer apps/games content.
- **Acceptance criteria:** (1) A custom `/404` page renders via the base layout with links back to the core pages. (2) `robots.txt` is served and references the sitemap. (3) A sitemap (`/sitemap-index.xml`) is generated at build for all routes. (4) A favicon is served and linked from every page `<head>`. (5) `npm run build`, `npm run check`, `npm test`, `npm audit`, and `python3 scripts/validate-governance.py` all pass; still fully static, no backend/runtime service.
- **Non-goals:** Product/apps-games content changes; visual redesign; real image assets; deployment execution (M4); analytics; a contact form.
- **Evidence method:** command/log artifact (build output listing 404 + sitemap; curl of robots.txt/sitemap/favicon/404) + runtime smoke screenshot of the 404 page.
- **Proof classification:** machine-verifiable.
- **Required checks:** `npm run build`; `npm run check`; `npm test`; `npm audit`; `python3 scripts/validate-governance.py`; runtime route smoke.
- **Issue links:** none open at creation; file findings in `docs/issues/LEDGER.md` or GitHub Issues.
- **Rollback/containment:** work on `feature/STEP-0003-site-essentials`; revert the feature merge commit (`git revert -m 1 <sha>`) or branch from `v0.2.0`; no data/production impact (static, no user data).
- **Evidence after completion:** All merge-critical gates Pass (2026-07-02); released as v0.3.0. `npm run build` → 11 routes + `404.html`, `robots.txt`, `sitemap-index.xml`/`sitemap-0.xml` (10 locs), `favicon.svg`; `npm run check` → 0/0/0; `npm test` → 28 passed; `npm audit` → 0 vulnerabilities; `python3 scripts/validate-governance.py` → 40/40 exit 0. Runtime smoke: robots/sitemap/favicon HTTP 200, unknown route → HTTP 404 custom page (screenshot captured), no console errors. Discussion checkpoint DISC-0001 run (fixed CI `npm test` gap; logged LEDGER-001).
