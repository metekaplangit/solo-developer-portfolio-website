# Task Card — STEP-0014

- **ID:** STEP-0014
- **Type:** feature (UI polish)
- **One outcome:** Raise site-wide polish — make layout **consistent and aligned** (one shared content measure, no misaligned or mis-spaced elements) and make the **main privacy page unambiguously the *website* policy** so it isn't confused with the apps (some of which collect data).
- **Why now:** The user wants a higher-quality, more polished feel: everything lining up, nothing sticking out, consistent across pages.
- **Findings (audit 2026-07-02):** Content pages re-clamp to ad-hoc widths — home 46rem, apps detail 48rem, about/support/privacy/PageHeader 44rem, 404 40rem — so text measures and right edges don't line up page to page. Apps detail footer has a `·Support` separator with a missing space. The same product's icon differs between its detail page (4rem, `--radius`, `shadow-sm`) and privacy page (4.5rem, `--radius-lg`, `shadow-md`).
- **Acceptance criteria:** (1) A shared `--maxw-prose` token defines the reading column; every content page (PageHeader, about, support, privacy index, privacy article + back link, apps detail + back link, 404) uses it, so measures and left/right edges match across pages. (2) Showcase pages (home, apps catalog) keep the wide container + grids. (3) The `·Support` separator (and any sibling) renders with correct spacing. (4) The Sole Focus icon is presented identically on its detail and privacy pages (same size, radius, shadow). (5) No horizontal overflow at desktop **or** mobile (scrollWidth == clientWidth). (6) The main privacy page (`global` policy) is titled and framed as the **MetKap Studio Website** privacy policy, states up front that apps/games are separate and some collect data, and links their own policies. (7) `npm run build`, `npm run check`, `npm test`, `python3 scripts/validate-governance.py` pass; contrast + semantic structure preserved.
- **Non-goals:** New sections/pages, copy rewrites, a light theme, animation overhauls, real screenshot pipeline.
- **Evidence method:** command/log artifact (build/check/test/validator) + before/after runtime screenshots (desktop + mobile) + a scripted overflow check per page.
- **Proof classification:** machine-verifiable + manual-runtime (visual quality is human-judged).
- **Required checks:** `npm run build`; `npm run check`; `npm test`; `python3 scripts/validate-governance.py`; runtime visual + overflow scan.
- **Issue links:** LEDGER-001 (avatar dup — may be touched here), LEDGER-002 (a11y CI — unrelated).
- **Rollback/containment:** work on `feature/STEP-0014-ui-polish`; revert the merge commit or branch from `v0.13.x`; CSS/token + component style only.
- **Evidence after completion:** All merge-critical gates Pass (2026-07-02); released as v0.14.0. `npm test` → 34 passed; `npm run check` → 0 errors/0 warnings; `npm run build` → 8 routes; `python3 scripts/validate-governance.py` → 40/40 exit 0. Added `--maxw-prose` (44rem); every content page (PageHeader, about, support, privacy index + article + back, apps detail + back, 404) shares it. Fixed the `·Support` separator (now `Privacy policy · Support`). Product icon unified across detail + privacy (72px, `--radius-lg`, `--shadow-md`). Overflow scan: all 8 pages, desktop + mobile (375px) → scrollWidth == clientWidth, no offenders. Main privacy page retitled "MetKap Studio Website Privacy Policy" with an explicit website-vs-apps opening and an "App & game privacy policies" section. No scheduled checkpoint at feature step 14 — but **Markdown Consistency is due after step 14** per cadence; run as MC-0007 next.
